services:
  # app:
  #   build: .
  #   container_name: grafana-recap-web
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   env_file:
  #     - .env
  #   environment:
  #     - DB_HOST=postgres
  #     - NODE_ENV=production
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - app-logs:/app/log

  postgres:
    image: docker.io/postgres:16-alpine
    container_name: grafana-recap-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-grafana_recap}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # sr-gen-api:
  #   build:
  #     context: apps/
  #     dockerfile: sr-generator/Dockerfile
  #   container_name: sr-gen-api
  #   restart: unless-stopped
  #   ports:
  #     - "8321:8321"
  #   environment:
  #     - SR_GEN_ENV=production
  #     # Point at the same Postgres used by the web app
  #     - SR_GEN_DATABASE_URL=postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@postgres:5432/${DB_NAME:-grafana_recap}
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - sr-gen-generated:/app/generated

volumes:
  pgdata:
  # app-logs:
  # sr-gen-generated:
