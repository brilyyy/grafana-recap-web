[project]
name = "bptx"
version = "0.1.0"
description = "Business PowerPoint Template Framework"
requires-python = ">=3.13"
dependencies = [
    "python-pptx>=1.0.2",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
bptx = ["py.typed"]
