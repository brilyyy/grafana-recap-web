{
  "name": "sr-generator",
  "private": true,
  "scripts": {
    "dev": "uv sync --extra api --quiet && PYTHONPATH=src uv run uvicorn api:app --reload --port 8321 --host 0.0.0.0",
    "sync": "uv sync --extra api",
    "lint": "uv sync --extra api --quiet && uv run ruff check src/",
    "test": "uv sync --extra api --quiet && uv run pytest tests/"
  }
}
